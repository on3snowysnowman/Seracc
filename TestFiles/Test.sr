
type struct Structy
{
    mut x: int;
}

export fn main() -> int
{
    let obj: * Structy = nullptr;

    let mut num: int = obj->x;
    let ptr: * int = &num;

    let result: int = (((*++ptr) / *ptr) + 1); 

    let arr: *mut * *mut * int[1][2];

    ret 0;
}