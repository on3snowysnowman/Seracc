

module SomeSubModule
{

export type struct HiddenStruct
{
    mut x: int;
}

export fn some_func() -> int
{
    let mut x: int = 0;

    ret x;
} 

} // module SomeSubModule

type struct Structy
{
    value: int;
}

type component Compy
{
    pub value: int;
}

fn main() -> void
{
    let obj: Structy = { .value = 0 };
    // let obj2: HiddenStruct = { .x = 0 }; // Should error on SymbolResolver

    let immut_ref: ref int = obj.value; 

    (*(ptr + 1)).value = 0;

    let result: int = 3 + 3.1;

    // (obj + obj).value = 9;

    let mut_ref: ref mut int = obj.value; // Should error on type checker

    let mut some_int: int = 312;
}