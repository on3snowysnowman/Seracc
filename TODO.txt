
Fix arr init expressions to parse as multiple nested curly braces when there is multiple array depths

Change parse expression to not create many TypeDecl for int literals. Instead have a single 
    int lit type_decl and just return that. ?? 